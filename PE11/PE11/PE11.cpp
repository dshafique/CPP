#include <cstdlib>
#include <iostream>

using namespace std;

class dateType;
{
public:
	void setDate(int September, int 15, int 2009);
	int getDay(15) const;
	int getMonth(September) const;
	int getYear(2009) const;
	void printDate(08 - 15 - 2009) const;
	dateType(int month = 1, int year = 1900);

	print("Today's date is %i %i, %i.", GetMonth(), GetDay(), GetYear());
};

int main(int argc, char *argv[])
{
	system("PAUSE");
	return EXIT_SUCCESS;
}